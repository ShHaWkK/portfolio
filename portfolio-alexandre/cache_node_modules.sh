#!/bin/sh
set -e
npm ci --legacy-peer-deps

# Remove any existing cache
rm -f node_modules.tar.gz

# Ensure node_modules exists before creating the archive
if [ -d node_modules ]; then
  tar -czf node_modules.tar.gz node_modules
fi