{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "installCommand": "if [ -f node_modules.tar.gz ]; then tar -xzf node_modules.tar.gz; else npm install --legacy-peer-deps; fi",
        "buildCommand": "npm run build && cp index.html dist/"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
